function calcAdd() {
    let a = +prompt(`First number`); 
    let b = +prompt(`Second number`);
    
    alert(`Result ${a + b}`);
}

function calcSub() {
    let a = +prompt(`First number`); 
    let b = +prompt(`Second number`);

    alert(`Result ${a - b}`);
}

function calcMult() {
    let a = +prompt(`First number`); 
    let b = +prompt(`Second number`);

    alert(`Result ${a * b}`);
}

function calcDiv() {
    let a = +prompt(`First number`); 
    let b = +prompt(`Second number`);

    alert(`Result ${a / b}`);
}

